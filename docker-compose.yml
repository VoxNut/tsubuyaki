services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - backend/.env
    ports:
      - "7860:7860"
    volumes:
      - hf-cache:/home/app/.cache/huggingface
    restart: unless-stopped

volumes:
  hf-cache:
